# models/

Mongoose schemas — the blueprint for what a "Customer", "ShopOwner", and "LoyaltyTransaction" look like in MongoDB.

Example (later milestones): `Customer.js`, `ShopOwner.js`, `LoyaltyTransaction.js`.

A model = the shape + rules of your data (required fields, data types, defaults).
