# controllers/

The actual logic for each route: read the request, talk to the database (via models), send back a response.

Example (later milestones): `customerController.js`, `ownerController.js`, `loyaltyController.js`.

Routes say WHERE a request goes; controllers say WHAT happens with it.
