# components/

Small, reusable UI pieces used across multiple pages.

Example (later milestones): `Navbar.jsx`, `LoyaltyProgressBar.jsx`, `CustomerCard.jsx`, `QRCodeDisplay.jsx`.

Rule of thumb: if a piece of UI is used in more than one page, or it makes a page file too long, it becomes a component.
