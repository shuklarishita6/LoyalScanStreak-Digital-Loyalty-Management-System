# pages/

Full screens the user navigates to (one file = one route).

Example (later milestones): `Home.jsx`, `CustomerLogin.jsx`, `CustomerRegister.jsx`, `CustomerDashboard.jsx`, `OwnerLogin.jsx`, `OwnerDashboard.jsx`.

Pages are built by combining components from `components/`.
