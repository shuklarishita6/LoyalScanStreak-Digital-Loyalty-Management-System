# utils/

Small, reusable helper functions that don't belong to any single model, route,
or controller.

- `generateToken.js` — creates a JWT for a logged-in user.
- `generateId.js` — generates a unique CUS#### / SHOP#### id at registration time.
